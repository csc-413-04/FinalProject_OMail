const initialState = {
    selectedEmail: null,

};

const selectedMailReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'SELECTED_MAIL':
            return Object.assign

    }


}




export default selectedMailReducer;